staking project :

- pool
- user will send nft into pool
- starttime
- endtime
  feature when run :
- send to pool (stake) : event make stake
- duration : != endtime
  ex : duration : the enable with draw day
